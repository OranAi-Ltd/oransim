name: Docs

# Build the landing page (root index.html + assets/) + MkDocs site, merge
# them into a single artifact, and deploy to GitHub Pages.
#
# Final routing:
#   /                → root index.html (project landing)
#   /assets/*        → static assets (SVG / screenshots)
#   /docs/           → MkDocs Material site
#
# First deploy is self-bootstrapping: configure-pages has enablement: true so
# we don't need the manual Settings → Pages → Source = "GitHub Actions" step.

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - '.github/workflows/docs.yml'
      - 'README.md'
      - 'index.html'
      - 'assets/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-docs
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install MkDocs + Material
        run: pip install "mkdocs>=1.6" "mkdocs-material>=9.5"

      - name: Build MkDocs to _site/docs
        run: mkdocs build --strict --site-dir _site/docs

      - name: Copy landing page + root assets
        run: |
          cp index.html _site/index.html
          cp -r assets _site/assets

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          # Auto-enable Pages on first run so we don't need the manual
          # Settings → Pages → Source = "GitHub Actions" click. Without this
          # the action returns HttpError: Not Found when Pages is disabled.
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
